import io as outr
import numpy as np
import numpySolver as npt

# does everything
def centrality():
#     executor.runTpfor(karate)
#     output = outr.readOut()
#     highest = utils.highestEigen(output)
#     plotter.plotCentrality(highest)


def laplacian():
    # l = matrixBuilder.buildLaplacian(karate)
    # runTpFor(l)
    # outr.read(out)
    # c = calculateCorrelation()
    # plotter.plotCorrelation(c, enunciado)